<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ld.poetry.dao.HistoryInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ld.poetry.entity.HistoryInfo">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="ip" property="ip"/>
        <result column="nation" property="nation"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="page_uri" property="pageUri"/>
        <result column="referer" property="referer"/>
        <result column="user_agent" property="userAgent"/>
        <result column="ua_type" property="uaType"/>
        <result column="ua_name" property="uaName"/>
        <result column="bot_verify_status" property="botVerifyStatus"/>
        <result column="bot_verify_reason" property="botVerifyReason"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, ip, nation, province, city, page_uri, referer, user_agent,
        ua_type, ua_name, bot_verify_status, bot_verify_reason, create_time
    </sql>

    <!-- 批量插入访问记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO history_info (
            user_id, ip, nation, province, city, page_uri, referer, user_agent,
            ua_type, ua_name, bot_verify_status, bot_verify_reason, create_time
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.userId}, #{item.ip}, #{item.nation}, #{item.province}, #{item.city}, #{item.pageUri},
                #{item.referer}, #{item.userAgent}, #{item.uaType}, #{item.uaName}, #{item.botVerifyStatus},
                #{item.botVerifyReason}, #{item.createTime}
            )
        </foreach>
    </insert>

</mapper>
